# react-json-page-bootstrap
React lib based on react-json-page and reactstrap to render pages from json using Bootstrap struture.
## Installation
Run the following command:
`npm install react-json-page-bootstrap`